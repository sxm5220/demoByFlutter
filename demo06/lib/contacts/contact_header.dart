import 'package:demo06/contacts/contact_item.dart';
import 'package:flutter/material.dart';

class ContactHeader extends StatelessWidget {
  const ContactHeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        //ContactItem(titleName: '新加好友', imageName: 'icon_addfriend.png'),
        //ContactItem(titleName: '公共聊天室', imageName: 'icon_groupchat.png'),
      ],
    );
  }
}
